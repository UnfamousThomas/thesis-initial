name: Operator E2E Tests

on:
  pull_request:
    paths-ignore:
      - 'operator/**'
      - '.github/workflows/test.yml' # re-run if workflow changes

jobs:
  e2e-test:
    runs-on: ubuntu-latest

    steps:
      - run: echo "No relevant changes detected."
